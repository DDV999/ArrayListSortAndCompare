import java.util.Comparator;

public class ComparatorTemp5 {
    public static void main(String[] args) {
//        Comparator<ComparatorTemp5> comparator = new Comparator<ComparatorTemp5>() {
//            @Override
//            public int compare(ComparatorTemp5 o1, ComparatorTemp5 o2) {
//                return o1.getId().compareTo(o2.getId());
//            }
//        };

//        Comparator<ComparatorTemp5> comparator = ((o1, o2) -> o1.getId().compareTo(o2.getId()));

//        Comparator<ComparatorTemp5> comparator = Comparator.comparing(obj ->obj.getId());

//        Comparator<Message> comparator = Comparator.comparing(obj -> obj.getId());
//        comparator = comparator.thenComparing(obj -> obj.getMessage().length());
    }
}
